# Test dependencies for dnsdiag test suite

# Core testing framework
pytest>=9.1.0

# Additional pytest plugins (optional but recommended)
pytest-timeout>=2.4.0           # Test timeout handling
pytest-xdist>=3.8.0            # Parallel test execution
pytest-cov>=7.1.0              # Code coverage reporting
pytest-html>=4.2.0             # HTML test reports
pytest-json-report>=1.5.0      # JSON test reports

# For CI/CD integration
pytest-github-actions-annotate-failures>=0.4.0  # GitHub Actions integration (optional)

# Performance and profiling (optional)
pytest-benchmark>=5.2.3        # Performance benchmarking (optional)

# Better assertion introspection
pytest-clarity>=1.0.1          # Better assertion failure messages (optional)